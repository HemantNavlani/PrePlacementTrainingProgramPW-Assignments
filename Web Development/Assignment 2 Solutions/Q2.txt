Q.2 What are the Different Types of Selectors in CSS & what are the advantages of them?

The simple selectors are based on class, id,etc.
The important ones are pseudo selectors which consists of pseudo class and pseudo elements 
Pseudo class is used after writing a : symbol and the examples are hover, link, etc.
Pseudo elements are used after writing a :: symbol and the examples are before, after, etc.
